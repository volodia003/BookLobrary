<Window x:Class="HomeLibrary.Views.LoansWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление займами книг"
        Height="650" Width="1100"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🔄 Управление займами книг" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Фильтр -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="Все займы" Style="{StaticResource SecondaryButton}" 
                    Click="ShowAllLoansButton_Click" Margin="0,0,10,0" Padding="15,8"/>
            <Button Content="Активные займы" Style="{StaticResource PrimaryButton}" 
                    Click="ShowActiveLoansButton_Click" Padding="15,8"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="LoansDataGrid" SelectionChanged="LoansDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Книга" Binding="{Binding BookTitle}" Width="*" MinWidth="200"/>
                <DataGridTextColumn Header="Кому" Binding="{Binding BorrowerName}" Width="150"/>
                <DataGridTextColumn Header="Дата займа" Binding="{Binding LoanDate, StringFormat='dd.MM.yyyy'}" Width="110"/>
                <DataGridTextColumn Header="Срок возврата" Binding="{Binding DueDate, StringFormat='dd.MM.yyyy'}" Width="110"/>
                <DataGridTextColumn Header="Дата возврата" Binding="{Binding ReturnDate, StringFormat='dd.MM.yyyy'}" Width="110"/>
                <DataGridTextColumn Header="Заметки" Binding="{Binding Notes}" Width="200"/>
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsOverdue}" Value="True">
                            <Setter Property="Background" Value="#FFEBEE"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,15,0,0">
            <Button Content="➕ Добавить займ" Style="{StaticResource PrimaryButton}" 
                    Click="AddButton_Click" Margin="0,0,10,0" Padding="15,8"/>
            <Button Content="✓ Отметить возврат" Style="{StaticResource SecondaryButton}" 
                    Click="ReturnButton_Click" x:Name="ReturnButton" IsEnabled="False" Margin="0,0,10,0" Padding="15,8"/>
            <Button Content="✏ Изменить" Style="{StaticResource SecondaryButton}" 
                    Click="EditButton_Click" x:Name="EditButton" IsEnabled="False" Margin="0,0,10,0" Padding="15,8"/>
            <Button Content="🗑 Удалить" Style="{StaticResource SecondaryButton}" 
                    Click="DeleteButton_Click" x:Name="DeleteButton" IsEnabled="False" Margin="0,0,20,0" Padding="15,8"/>
            <Button Content="Закрыть" Style="{StaticResource SecondaryButton}" 
                    Click="CloseButton_Click" Padding="15,8"/>
        </StackPanel>
    </Grid>
</Window>
