<Window x:Class="HomeLibrary.Views.BooksWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление книгами"
        Height="700" Width="1100"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="📖 Управление книгами" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Поиск и фильтры -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect Color="Gray" Opacity="0.2" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Поиск -->
                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Поиск (название, автор, ISBN):" FontSize="12" Margin="0,0,0,5"/>
                    <TextBox x:Name="SearchTextBox" Height="30" />
                </StackPanel>

                <!-- Фильтр по жанру -->
                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,10,0" Width="150">
                    <TextBlock Text="Жанр:" FontSize="12" Margin="0,0,0,5"/>
                    <ComboBox x:Name="GenreFilterComboBox" Height="30" DisplayMemberPath="Name"/>
                </StackPanel>

                <!-- Фильтр по статусу -->
                <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,10,0" Width="130">
                    <TextBlock Text="Статус:" FontSize="12" Margin="0,0,0,5"/>
                    <ComboBox x:Name="StatusFilterComboBox" Height="30"/>
                </StackPanel>

                <!-- Фильтр по году -->
                <StackPanel Grid.Row="0" Grid.Column="3" Margin="0,0,10,0" Width="100">
                    <TextBlock Text="Год издания:" FontSize="12" Margin="0,0,0,5"/>
                    <TextBox x:Name="YearFilterTextBox" Height="30"/>
                </StackPanel>

                <!-- Кнопка поиска -->
                <Button Grid.Row="0" Grid.Column="4" Content="🔍 Найти" Style="{StaticResource PrimaryButton}" 
                        Click="SearchButton_Click" VerticalAlignment="Bottom" Height="30" Padding="15,5"/>

                <!-- Кнопка сброса -->
                <Button Grid.Row="1" Grid.Column="4" Content="Сбросить" Style="{StaticResource SecondaryButton}" 
                        Click="ResetButton_Click" Margin="0,10,0,0" Height="30" Padding="15,5"/>
            </Grid>
        </Border>

        <!-- Список книг -->
        <DataGrid Grid.Row="2" x:Name="BooksDataGrid" 
                  SelectionChanged="BooksDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="*" MinWidth="200"/>
                <DataGridTextColumn Header="Автор" Binding="{Binding AuthorName}" Width="150"/>
                <DataGridTextColumn Header="Жанр" Binding="{Binding GenreName}" Width="120"/>
                <DataGridTextColumn Header="ISBN" Binding="{Binding ISBN}" Width="130"/>
                <DataGridTextColumn Header="Год" Binding="{Binding PublicationYear}" Width="70"/>
                <DataGridTextColumn Header="Место хранения" Binding="{Binding LocationName}" Width="140"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки управления -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,15,0,0">
            <Button Content="➕ Добавить" Style="{StaticResource PrimaryButton}" 
                    Click="AddButton_Click" Margin="0,0,10,0" Padding="15,8"/>
            <Button Content="✏ Изменить" Style="{StaticResource SecondaryButton}" 
                    Click="EditButton_Click" x:Name="EditButton" IsEnabled="False" Margin="0,0,10,0" Padding="15,8"/>
            <Button Content="🗑 Удалить" Style="{StaticResource SecondaryButton}" 
                    Click="DeleteButton_Click" x:Name="DeleteButton" IsEnabled="False" Margin="0,0,20,0" Padding="15,8"/>
            <Button Content="⭐ Добавить отзыв" Style="{StaticResource SecondaryButton}" 
                    Click="AddReviewButton_Click" x:Name="AddReviewButton" IsEnabled="False" Margin="0,0,10,0" Padding="15,8"/>
            <Button Content="📖 Посмотреть отзывы" Style="{StaticResource SecondaryButton}" 
                    Click="ViewReviewsButton_Click" x:Name="ViewReviewsButton" IsEnabled="False" Margin="0,0,20,0" Padding="15,8"/>
            <Button Content="Закрыть" Style="{StaticResource SecondaryButton}" 
                    Click="CloseButton_Click" Padding="15,8"/>
        </StackPanel>
    </Grid>
</Window>
