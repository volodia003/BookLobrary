<Window x:Class="HomeLibrary.Views.ReportsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Отчеты"
        Height="650" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="📊 Отчеты" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Выбор отчета -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Выберите отчет:" FontSize="14" FontWeight="SemiBold" 
                       VerticalAlignment="Center" Margin="0,0,15,0"/>
            <Button Content="Одолженные книги" Style="{StaticResource SecondaryButton}" 
                    Click="LoanedBooksButton_Click" Margin="0,0,10,0" Padding="15,8"/>
            <Button Content="Статистика чтения по годам" Style="{StaticResource SecondaryButton}" 
                    Click="ReadingByYearButton_Click" Margin="0,0,10,0" Padding="15,8"/>
            <Button Content="Статистика чтения по месяцам" Style="{StaticResource SecondaryButton}" 
                    Click="ReadingByMonthButton_Click" Padding="15,8"/>
        </StackPanel>

        <!-- Результаты отчета -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="20">
            <Border.Effect>
                <DropShadowEffect Color="Gray" Opacity="0.2" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>
            
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock x:Name="ReportTitleText" Text="Выберите отчет из списка выше" 
                               FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
                    
                    <DataGrid x:Name="ReportDataGrid" Visibility="Collapsed" MaxHeight="400"/>
                    
                    <TextBlock x:Name="ReportTextBlock" TextWrapping="Wrap" FontSize="14" LineHeight="24"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Кнопки -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="📄 Экспорт в текст" Style="{StaticResource SecondaryButton}" 
                    Click="ExportButton_Click" Margin="0,0,10,0" Padding="15,8"/>
            <Button Content="Закрыть" Style="{StaticResource SecondaryButton}" 
                    Click="CloseButton_Click" Padding="15,8"/>
        </StackPanel>
    </Grid>
</Window>
