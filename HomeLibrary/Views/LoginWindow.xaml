<Window x:Class="HomeLibrary.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Домашняя библиотека - Вход в систему"
        Height="634" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="0,0,0,-483">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#2196F3" Padding="20">
            <StackPanel>
                <TextBlock Text="📚" FontSize="48" HorizontalAlignment="Center" Foreground="White"/>
                <TextBlock Text="Домашняя библиотека" FontSize="24" FontWeight="Bold" 
                           HorizontalAlignment="Center" Foreground="White" Margin="0,10,0,0"/>
                <TextBlock Text="Система учета книг" FontSize="14" 
                           HorizontalAlignment="Center" Foreground="White" Opacity="0.9" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Форма входа -->
        <Border Grid.Row="1" Background="White" Margin="40,40,40,559" Padding="30" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect Color="Gray" Opacity="0.3" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>
            
            <StackPanel>
                <TextBlock Text="Вход в систему" FontSize="18" FontWeight="SemiBold" 
                           HorizontalAlignment="Center" Margin="0,0,0,25"/>

                <TextBlock Text="Логин:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="UsernameTextBox" Height="35" FontSize="14" Margin="0,0,0,15"/>

                <TextBlock Text="Пароль:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox" Height="35" FontSize="14" Margin="0,0,0,20"/>

                <Button Content="Войти" Style="{StaticResource PrimaryButton}" 
                        Height="40" Click="LoginButton_Click" Margin="0,0,0,10"/>

                <TextBlock x:Name="ErrorMessage" Foreground="Red" TextAlignment="Center" 
                           Margin="0,10,0,0" TextWrapping="Wrap" Visibility="Collapsed"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
