<Window x:Class="HomeLibrary.Views.AuthorsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление авторами"
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="✍ Управление авторами" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <DataGrid Grid.Row="1" x:Name="AuthorsDataGrid" SelectionChanged="AuthorsDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="150"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="150"/>
                <DataGridTextColumn Header="Страна" Binding="{Binding Country}" Width="120"/>
                <DataGridTextColumn Header="Дата рождения" Binding="{Binding BirthDate, StringFormat='dd.MM.yyyy'}" Width="120"/>
                <DataGridTextColumn Header="Биография" Binding="{Binding Biography}" Width="*" MinWidth="200"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,15,0,0">
            <Button Content="➕ Добавить" Style="{StaticResource PrimaryButton}" 
                    Click="AddButton_Click" Margin="0,0,10,0" Padding="15,8"/>
            <Button Content="✏ Изменить" Style="{StaticResource SecondaryButton}" 
                    Click="EditButton_Click" x:Name="EditButton" IsEnabled="False" Margin="0,0,10,0" Padding="15,8"/>
            <Button Content="🗑 Удалить" Style="{StaticResource SecondaryButton}" 
                    Click="DeleteButton_Click" x:Name="DeleteButton" IsEnabled="False" Margin="0,0,20,0" Padding="15,8"/>
            <Button Content="Закрыть" Style="{StaticResource SecondaryButton}" 
                    Click="CloseButton_Click" Padding="15,8"/>
        </StackPanel>
    </Grid>
</Window>
