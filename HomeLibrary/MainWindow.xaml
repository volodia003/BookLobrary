<Window x:Class="HomeLibrary.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Домашняя библиотека"
        Height="850" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Border Grid.Row="0" Background="#2196F3" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="📚" FontSize="28" Foreground="White" VerticalAlignment="Center"/>
                    <TextBlock Text="Домашняя библиотека" FontSize="22" FontWeight="Bold" 
                               Foreground="White" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock x:Name="UserNameText" FontSize="14" Foreground="White" 
                               VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <Button Content="Выход" Style="{StaticResource SecondaryButton}" 
                            Click="LogoutButton_Click" Padding="10,5"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Основное содержимое -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Боковое меню -->
            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="0" Margin="0,0,10,0">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" Opacity="0.2" BlurRadius="10" ShadowDepth="2"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Меню" FontSize="18" FontWeight="Bold" 
                               Padding="20,20,20,15" Background="#F5F5F5"/>

                    <Button Content="📖 Книги" Style="{StaticResource SecondaryButton}" 
                            Click="BooksButton_Click" Height="50" HorizontalContentAlignment="Left" 
                            Padding="20,0" Margin="0" BorderThickness="0,0,0,1"/>

                    <Button Content="✍ Авторы" Style="{StaticResource SecondaryButton}" 
                            Click="AuthorsButton_Click" Height="50" HorizontalContentAlignment="Left" 
                            Padding="20,0" Margin="0" BorderThickness="0,0,0,1"/>

                    <Button Content="🏷 Жанры" Style="{StaticResource SecondaryButton}" 
                            Click="GenresButton_Click" Height="50" HorizontalContentAlignment="Left" 
                            Padding="20,0" Margin="0" BorderThickness="0,0,0,1"/>

                    <Button Content="📍 Места хранения" Style="{StaticResource SecondaryButton}" 
                            Click="LocationsButton_Click" Height="50" HorizontalContentAlignment="Left" 
                            Padding="20,0" Margin="0" BorderThickness="0,0,0,1"/>

                    <Button Content="🔄 Займы книг" Style="{StaticResource SecondaryButton}" 
                            Click="LoansButton_Click" Height="50" HorizontalContentAlignment="Left" 
                            Padding="20,0" Margin="0" BorderThickness="0,0,0,1"/>

                    <Button Content="📊 Отчеты" Style="{StaticResource SecondaryButton}" 
                            Click="ReportsButton_Click" Height="50" HorizontalContentAlignment="Left" 
                            Padding="20,0" Margin="0" BorderThickness="0,0,0,1"/>

                    <Button Content="📈 Статистика" Style="{StaticResource SecondaryButton}" 
                            Click="StatisticsButton_Click" Height="50" HorizontalContentAlignment="Left" 
                            Padding="20,0" Margin="0" BorderThickness="0,0,0,1"/>

                    <Button x:Name="UsersButton" Content="👥 Пользователи" Style="{StaticResource SecondaryButton}" 
                            Click="UsersButton_Click" Height="50" HorizontalContentAlignment="Left" 
                            Padding="20,0" Margin="0" BorderThickness="0,0,0,1"/>
                </StackPanel>
            </Border>

            <!-- Основная панель - Главная страница -->
            <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="30">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" Opacity="0.2" BlurRadius="10" ShadowDepth="2"/>
                </Border.Effect>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Добро пожаловать!" FontSize="28" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBlock x:Name="WelcomeText" FontSize="16" Foreground="#666" Margin="0,0,0,30" TextWrapping="Wrap"/>

                        <TextBlock Text="Статистика библиотеки" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,20"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Карточка: Всего книг -->
                            <Border Grid.Column="0" Background="#E3F2FD" CornerRadius="8" Padding="20" Margin="0,0,10,10">
                                <StackPanel>
                                    <TextBlock Text="📚" FontSize="32" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="TotalBooksText" Text="0" FontSize="36" FontWeight="Bold" 
                                               HorizontalAlignment="Center" Foreground="#1976D2" Margin="0,10,0,5"/>
                                    <TextBlock Text="Всего книг" FontSize="14" HorizontalAlignment="Center" Foreground="#666"/>
                                </StackPanel>
                            </Border>

                            <!-- Карточка: Авторов -->
                            <Border Grid.Column="1" Background="#FFF3E0" CornerRadius="8" Padding="20" Margin="0,0,10,10">
                                <StackPanel>
                                    <TextBlock Text="✍" FontSize="32" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="TotalAuthorsText" Text="0" FontSize="36" FontWeight="Bold" 
                                               HorizontalAlignment="Center" Foreground="#F57C00" Margin="0,10,0,5"/>
                                    <TextBlock Text="Авторов" FontSize="14" HorizontalAlignment="Center" Foreground="#666"/>
                                </StackPanel>
                            </Border>

                            <!-- Карточка: Жанров -->
                            <Border Grid.Column="2" Background="#F3E5F5" CornerRadius="8" Padding="20" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="🏷" FontSize="32" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="TotalGenresText" Text="0" FontSize="36" FontWeight="Bold" 
                                               HorizontalAlignment="Center" Foreground="#7B1FA2" Margin="0,10,0,5"/>
                                    <TextBlock Text="Жанров" FontSize="14" HorizontalAlignment="Center" Foreground="#666"/>
                                </StackPanel>
                            </Border>

                            <!-- Карточка: В наличии -->
                            <Border Grid.Column="0" Grid.Row="1" Background="#E8F5E9" CornerRadius="8" Padding="20" Margin="0,0,10,0">
                                <StackPanel>
                                    <TextBlock Text="✓" FontSize="32" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="AvailableBooksText" Text="0" FontSize="36" FontWeight="Bold" 
                                               HorizontalAlignment="Center" Foreground="#388E3C" Margin="0,10,0,5"/>
                                    <TextBlock Text="В наличии" FontSize="14" HorizontalAlignment="Center" Foreground="#666"/>
                                </StackPanel>
                            </Border>

                            <!-- Карточка: Одолжено -->
                            <Border Grid.Column="1" Grid.Row="1" Background="#FFEBEE" CornerRadius="8" Padding="20" Margin="0,0,10,0">
                                <StackPanel>
                                    <TextBlock Text="🔄" FontSize="32" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="LoanedBooksText" Text="0" FontSize="36" FontWeight="Bold" 
                                               HorizontalAlignment="Center" Foreground="#C62828" Margin="0,10,0,5"/>
                                    <TextBlock Text="Одолжено" FontSize="14" HorizontalAlignment="Center" Foreground="#666"/>
                                </StackPanel>
                            </Border>

                            <!-- Карточка: Пользователей -->
                            <Border Grid.Column="2" Grid.Row="1" Background="#E0F2F1" CornerRadius="8" Padding="20" Margin="0,0,0,0">
                                <StackPanel>
                                    <TextBlock Text="👥" FontSize="32" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="TotalUsersText" Text="0" FontSize="36" FontWeight="Bold" 
                                               HorizontalAlignment="Center" Foreground="#00796B" Margin="0,10,0,5"/>
                                    <TextBlock Text="Пользователей" FontSize="14" HorizontalAlignment="Center" Foreground="#666"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <TextBlock Text="Быстрый доступ" FontSize="20" FontWeight="SemiBold" Margin="0,40,0,15"/>

                        <WrapPanel>
                            <Button Content="➕ Добавить книгу" Style="{StaticResource PrimaryButton}" 
                                    Click="BooksButton_Click" Margin="0,0,10,0" Padding="20,10"/>
                            <Button Content="📋 Посмотреть займы" Style="{StaticResource SecondaryButton}" 
                                    Click="LoansButton_Click" Margin="0,0,10,0" Padding="20,10"/>
                            <Button Content="📊 Отчеты" Style="{StaticResource SecondaryButton}" 
                                    Click="ReportsButton_Click" Padding="20,10"/>
                        </WrapPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Нижняя панель -->
        <Border Grid.Row="1" Background="#2196F3" Padding="10" Margin="0,694,0,0">
            <TextBlock Text="© 2025 Домашняя библиотека. Система учета книг в домашней библиотеке." 
                       FontSize="12" Foreground="White" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
